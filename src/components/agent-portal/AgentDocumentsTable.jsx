import { FileText } from 'lucide-react';

export default function AgentDocumentsTable({ documents }) {
  return <div className="overflow-hidden rounded-xl border border-gray-200 bg-white"><div className="border-b border-gray-100 px-5 py-4"><h2 className="font-semibold text-gray-900">Customer Documents</h2></div>{documents.map(document => <div key={`${document.customer_id}-${document.file_url}-${document.label}`} className="flex items-center justify-between gap-4 border-b border-gray-100 px-5 py-4 last:border-0"><div className="flex min-w-0 items-center gap-3"><span className="rounded-lg bg-blue-50 p-2 text-blue-600"><FileText className="h-4 w-4" /></span><div className="min-w-0"><p className="truncate text-sm font-medium text-gray-800">{document.label || document.type || 'Document'}</p><p className="truncate text-xs text-gray-400">{document.customer_name}</p></div></div><div className="flex items-center gap-4"><span className="text-xs text-gray-500">{document.expiry_date ? `Expires ${document.expiry_date}` : 'No expiry date'}</span>{document.file_url && <a href={document.file_url} target="_blank" rel="noreferrer" className="text-xs font-medium text-blue-600 hover:text-blue-700">View</a>}</div></div>)}{!documents.length && <p className="px-5 py-12 text-center text-sm text-gray-400">No customer documents found.</p>}</div>;
}