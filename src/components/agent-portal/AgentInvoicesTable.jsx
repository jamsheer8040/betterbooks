const fmt = value => (value || 0).toLocaleString('en-US', { minimumFractionDigits: 2, maximumFractionDigits: 2 });
const date = value => value ? new Date(value).toLocaleDateString('en-GB', { day: '2-digit', month: 'short', year: 'numeric' }) : '—';

export default function AgentInvoicesTable({ invoices, payments }) {
  const paidFor = invoice => payments.filter(payment => payment.invoice_id === invoice.id).reduce((sum, payment) => sum + (payment.amount || 0), 0);
  return <div className="overflow-x-auto rounded-xl border border-gray-200 bg-white"><table className="min-w-full text-sm"><thead className="bg-gray-50 text-left text-xs font-semibold uppercase tracking-wide text-gray-500"><tr><th className="px-5 py-3">Document</th><th className="px-5 py-3">Customer</th><th className="px-5 py-3">Date</th><th className="px-5 py-3">Total</th><th className="px-5 py-3">Payments</th><th className="px-5 py-3">Status</th></tr></thead><tbody className="divide-y divide-gray-100">{invoices.map(invoice => { const paid = paidFor(invoice); return <tr key={invoice.id}><td className="px-5 py-4"><p className="font-medium text-gray-900">{invoice.invoice_number || '—'}</p><p className="text-xs text-gray-400">{invoice.type === 'service_receipt' ? 'Receipt' : 'Invoice'}</p></td><td className="px-5 py-4 text-gray-600">{invoice.customer_name}</td><td className="px-5 py-4 text-gray-600">{date(invoice.invoice_date)}</td><td className="px-5 py-4 font-medium text-gray-900">AED {fmt(invoice.total)}</td><td className="px-5 py-4 text-green-700">AED {fmt(paid)}</td><td className="px-5 py-4"><span className="rounded-full bg-gray-100 px-2.5 py-1 text-xs font-medium capitalize text-gray-700">{invoice.status || 'draft'}</span></td></tr>; })}{!invoices.length && <tr><td colSpan="6" className="px-5 py-12 text-center text-gray-400">No invoices or payments are available.</td></tr>}</tbody></table></div>;
}